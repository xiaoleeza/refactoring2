package com.example.refactoring2.consolidate.duplicate.conditional.fragments;

public class Refactor {

	public static void main(String[] args) {
		/*if (isSpecialDeal()) {
			total = price * 0.95;
			send();
		} else {
			total = price * 0.98;
			send();
		}
		
		-->
		
		if (isSpecialDeal())
			total = price * 0.95;
		else
			total = price * 0.98;
		send();*/
			
	}
}
